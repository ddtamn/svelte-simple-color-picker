{
  "name": "svelte-simple-color-picker-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "format": "prettier --write .",
    "lint": "prettier --check . && eslint ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "eslint": "^9.0.0"
  }
}
